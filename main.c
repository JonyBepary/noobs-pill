// -lurlmon
#include "vcs_lib.h"

int main(int argc, char const *argv[])
{

    //
    calibrate();
    clear_screen();
    Start_print();
    // puts(getenv("PATH"));

    // printf("%d", system("gcc main.c -lurlmon -o main2"));
    // if (ret != 0)
    // {
    //     printf("tool is not here, move along\n");
    // }
    // if (system("wget"))
    // {
    // }

    // Download_Wget();

    // printf("sys: %d\n", );

    // Download_LIST_Through_wget();

    // printf("%s", urlencode("Hello / world ="));

    return 0;
}
